type Chamado = {
  clienteNome: string;
  dataAbertura: string;
  id: number;
  prioridade: string;
  status: string;
  titulo: string;
};

const chamados: Chamado[] = [

];
export { chamados, Chamado };
